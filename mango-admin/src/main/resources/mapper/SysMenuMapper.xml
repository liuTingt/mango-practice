<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.louis.mango.admin.dao.SysMenuMapper">


<select id="findByUserName" resultType="com.louis.mango.admin.model.SysMenu">
	SELECT m.* FROM `sys_menu` m 
	LEFT JOIN sys_role_menu rm ON rm.menu_id = m.id
	LEFT JOIN sys_user_role ur ON ur.role_id = rm.role_id
	LEFT JOIN sys_user u ON u.id = ur.user_id
	WHERE m.del_flag='0' AND u.`name` = #{userName}
</select>

<select id="findRoleMenus" resultType="com.louis.mango.admin.model.SysMenu">
	SELECT menu.* FROM `sys_menu` menu
	LEFT JOIN sys_role_menu rm ON rm.menu_id=menu.id
	WHERE rm.role_id=#{roleId}
</select>
</mapper>
