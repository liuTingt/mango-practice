server:
  port: 9090
  
spring:
  cache:
    type: redis
  redis:
    host: 127.0.0.1
    port: 6379
    password: 12345
    timeout: 6000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    database: 0
  session:
    store-type: redis  # session存储格式为redis
    redis:
      namespace: demo # session存储得命令空间  为了对session名称id做区分